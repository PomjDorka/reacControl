local config = {
    mode = "AUTO",
    rpmTarget = 900,
    matrixStart = 30,
    matrixStop = 85,
    flowStep = 5000,
    maxFlow = 1000000,
    rpmDeadband = 5,
    updateInterval = 1,
    turbineStartDelay = 3,
    reactorName = "back",
    matrixName = "inductionPort_0",
    monitorName = "monitor_1"
}

return config
