local config = {
    mode = "AUTO",
    steamTarget = 200000,
    rpmTarget = 900,
    matrixStart = 30,
    matrixStop = 85,
    rodStep = 0.1,
    flowStep = 5000,
    maxFlow = 1000000,
    rpmDeadband = 5,
    steamDeadband = 0.03,
    updateInterval = 1,
    turbineStartDelay = 3,
    reactorName = "back",
    matrixName = "inductionPort_0",
    monitorName = "monitor_1"
}

return config
